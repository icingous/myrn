import { colors } from "./colors";

export const iconButton = {
  width: 28,
  height: 28,
};

export const iconButtonRipple = {
  color: colors.light,
  borderless: true,
  radius: 20,
  foreground: true,
};

export const toolIconProps = {
  fill: colors.dark,
  stroke: colors.secondary,
};
